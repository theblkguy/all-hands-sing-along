import Config

# Copy to config/dev.secrets.exs (gitignored) and replace the value:
#   mix phx.gen.secret

config :all_hands_sing_along, AllHandsSingAlongWeb.Endpoint,
  secret_key_base: "replace-with-mix-phx-gen-secret"
